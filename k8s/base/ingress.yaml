apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aleclabs-home
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: aleclabs-home
            port:
              number: 80
